_target_: src.tracking.botsort_tracker.BotSortTracker
track_thresh: 0.5
low_thresh: 0.1
match_thresh: 0.8
track_buffer_frames: 30
min_hits_to_confirm: 3
use_camera_motion_compensation: true
appearance_weight: 0.0   # keep 0 until a Re-ID embedding model is wired in (see module docstring)
